apiVersion: v1
kind: Service
metadata:
  name: container-up
  namespace: nanobot
spec:
  type: NodePort
  selector:
    app: container-up
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080